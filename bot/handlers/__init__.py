from pyrogram import Client
from . import start, admin, bypass, subscription

def register_handlers(app: Client):
    """Register all bot handlers"""
    # This function is called from main.py to register handlers
    # Handlers are registered via decorators in their respective files
    pass
